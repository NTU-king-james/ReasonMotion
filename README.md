# ReasonMotion

We attatched the editor and predictor of ReasonMotion to this repository.

The specific details of the editor and predictor can be found in the editor and predictor directories.

Both can share the same environment by running `pip install -r requirements.txt`.